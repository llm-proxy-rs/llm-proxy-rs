host = "0.0.0.0"
port = 3000

[reasoning_effort_to_thinking_budget_tokens]
low = 1024
medium = 2048
high = 4096

# Prefixes used to detect inference profiles in model IDs.
inference_profile_prefixes = ["us."]
